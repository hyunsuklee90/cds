# Git이 텍스트 파일로 판단하는 파일은 기본적으로 EOL 변환을 하도록 설정 (core.autocrlf 설정에 따름)
* text=auto

# *.sh 파일은 텍스트로 간주하고, 체크아웃 및 커밋 시 EOL을 LF로 강제합니다.
# 이렇게 하면 Windows, Mac, Linux 어떤 환경에서도 해당 파일은 Git 저장소와 작업 디렉토리에서 모두 LF로 처리됩니다.
*.sh text eol=lf