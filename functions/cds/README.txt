#
# Add following lines to the ~/.bashrc

export PATH_CDS=/home/hyunsuk/.HS/functions  # Path where the load_functions.sh is located
source $PATH_CDS/load_functions.sh 

cds_save : save the stored cds data to the $PATH_CDS/data
cds_load : load data from $PATH_CDS/data

How to use cds: 
type cds -h 

